<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
    <configuration>
        <!-- report theme -->
        <!-- standard, dark -->
        <theme>standard</theme>
    
        <!-- document encoding -->
        <!-- defaults to UTF-8 -->
        <encoding>UTF-8</encoding>
        
        <!-- protocol for script and stylesheets -->
        <!-- defaults to https -->
        <protocol>http</protocol>
        
        <!-- title of the document -->
        <documentTitle>Automation Regression Test Report</documentTitle>
        
        <!-- report name - displayed at top-nav -->
        <reportName>Automation Regression Test Report</reportName>
        
        <!-- report headline - displayed at top-nav, after reportHeadline -->
        <reportHeadline></reportHeadline>
        
        <!-- global date format override -->
        <!-- defaults to yyyy-MM-dd -->
        <dateFormat>yyyy-MM-dd</dateFormat>
        
        <!-- global time format override -->
        <!-- defaults to HH:mm:ss -->
        <timeFormat>HH:mm:ss</timeFormat>
        
        <!-- custom javascript -->
        <scripts>
            <![CDATA[
			 $(document).ready(function()
				{
              	$('.dashboard-view').click();
              	$(".logo-container").html("Bol.com" );
              	$("div.card.accent.green-accent").removeClass("card accent green-accent").addClass("card accent white");
              	$("div.card.accent.pink-accent").removeClass("card accent pink-accent").addClass("card accent white");
              	$("span.test-started-time.label.green.lighten-1.text-white").removeClass("test-started-time label green lighten-1 text-white").addClass("test-started-time label grey lighten-1 text-white");
              	$("span.test-ended-time.label.red.lighten-1.text-white").removeClass("test-started-time label red lighten-1 text-white").addClass("test-started-time label grey lighten-1 text-white");
				});
            ]]>
        </scripts>
        
        <!-- custom styles -->
        <styles>
            <![CDATA[
             .pass-percentage.panel-lead { font-size:60px; font-weight:bold; margin-top:40px; }
             progress.light-blue.lighten-3 { margin-top: 0; }
             ]]>
        </styles>
    </configuration>
</extentreports>